{
  "name": "@quid2/ts-core",
  "version": "0.1.3",
  "description": "TypeScript API for Quid2, core functions",
  "main": "./js/index.js",
  "typings": "./js/index.d.ts",
  "scripts": {
    "build": "tsc",
    "release": "npm run build;npm run test;standard-version;git commit -am \"release\";git push --follow-tags origin master;git status"
  }
}
